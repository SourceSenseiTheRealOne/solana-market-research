// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/SourceSenseiTheRealOne/solana-market-research/ent/candidate"
	"github.com/SourceSenseiTheRealOne/solana-market-research/ent/socialsnapshot"
)

// SocialSnapshot is the model entity for the SocialSnapshot schema.
type SocialSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SocialEvidence holds the value of the "social_evidence" field.
	SocialEvidence map[string]interface{} `json:"social_evidence,omitempty"`
	// Score holds the value of the "score" field.
	Score int `json:"score,omitempty"`
	// ObservedAt holds the value of the "observed_at" field.
	ObservedAt time.Time `json:"observed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SocialSnapshotQuery when eager-loading is set.
	Edges                      SocialSnapshotEdges `json:"edges"`
	candidate_social_snapshots *int
	selectValues               sql.SelectValues
}

// SocialSnapshotEdges holds the relations/edges for other nodes in the graph.
type SocialSnapshotEdges struct {
	// Candidate holds the value of the candidate edge.
	Candidate *Candidate `json:"candidate,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CandidateOrErr returns the Candidate value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SocialSnapshotEdges) CandidateOrErr() (*Candidate, error) {
	if e.Candidate != nil {
		return e.Candidate, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: candidate.Label}
	}
	return nil, &NotLoadedError{edge: "candidate"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SocialSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case socialsnapshot.FieldSocialEvidence:
			values[i] = new([]byte)
		case socialsnapshot.FieldID, socialsnapshot.FieldScore:
			values[i] = new(sql.NullInt64)
		case socialsnapshot.FieldObservedAt, socialsnapshot.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case socialsnapshot.ForeignKeys[0]: // candidate_social_snapshots
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SocialSnapshot fields.
func (_m *SocialSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case socialsnapshot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case socialsnapshot.FieldSocialEvidence:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field social_evidence", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.SocialEvidence); err != nil {
					return fmt.Errorf("unmarshal field social_evidence: %w", err)
				}
			}
		case socialsnapshot.FieldScore:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field score", values[i])
			} else if value.Valid {
				_m.Score = int(value.Int64)
			}
		case socialsnapshot.FieldObservedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field observed_at", values[i])
			} else if value.Valid {
				_m.ObservedAt = value.Time
			}
		case socialsnapshot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case socialsnapshot.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field candidate_social_snapshots", value)
			} else if value.Valid {
				_m.candidate_social_snapshots = new(int)
				*_m.candidate_social_snapshots = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SocialSnapshot.
// This includes values selected through modifiers, order, etc.
func (_m *SocialSnapshot) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCandidate queries the "candidate" edge of the SocialSnapshot entity.
func (_m *SocialSnapshot) QueryCandidate() *CandidateQuery {
	return NewSocialSnapshotClient(_m.config).QueryCandidate(_m)
}

// Update returns a builder for updating this SocialSnapshot.
// Note that you need to call SocialSnapshot.Unwrap() before calling this method if this SocialSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SocialSnapshot) Update() *SocialSnapshotUpdateOne {
	return NewSocialSnapshotClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SocialSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SocialSnapshot) Unwrap() *SocialSnapshot {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SocialSnapshot is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SocialSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("SocialSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("social_evidence=")
	builder.WriteString(fmt.Sprintf("%v", _m.SocialEvidence))
	builder.WriteString(", ")
	builder.WriteString("score=")
	builder.WriteString(fmt.Sprintf("%v", _m.Score))
	builder.WriteString(", ")
	builder.WriteString("observed_at=")
	builder.WriteString(_m.ObservedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SocialSnapshots is a parsable slice of SocialSnapshot.
type SocialSnapshots []*SocialSnapshot
